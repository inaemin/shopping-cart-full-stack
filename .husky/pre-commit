git diff --cached --name-only | grep -q '^server/' && { (cd server && npx lint-staged) || exit 1; }
git diff --cached --name-only | grep -q '^client/' && { (cd client && npx lint-staged) || exit 1; }
exit 0
